#---------------------------------------------
#
# more examples
#
set dummy u,v;
set title "Yet Another Parametric Surface"

a=sur{ [cos(u)*(cos(v)+2)+sin(u)*(sin(v)+2),
         -cos(u)*(sin(v)+2)+sin(u)*(cos(v)+2), pi-v],
	[u=0:2*pi][v=0:2*pi]};
plot a;
set dummy x,y;
