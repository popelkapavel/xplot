#---------------------------------------------
#
# more examples
#
set dummy u,v;
set title "Yet Another Parametric Surface"
a=sur{[u*cos(v)*sin(u), u*cos(u)*cos(v), -u*sin(v)]
	[u=0:3*pi][v=0:pi]};
plot a;
set dummy x,y;
