#---------------------------------------------
#
# more examples
#
set dummy u,v;
set title "Yet Another Parametric Surface"

a=sur{[sin(u)*cos(v), sin(u)*sin(v),-0.25*cosh(u)]
	[u=0:pi][v=0:2*pi]};
plot a;
set dummy x,y;
